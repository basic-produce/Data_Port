The multiple files in Data needed to be appended.
In each file, the data are spread into multiple sheets for months.
The script will combine all of them into one file (combined_data) and add column Filename